services:
  server:
    build:
      context: .
      target: server
      args:
        EXAMPLE: ${EXAMPLE:-01_hello_server}
    container_name: socketlab-server
    ports:
      - "8080:8080"
    networks:
      - socketlab-net

  client:
    build:
      context: .
      target: client
      args:
        EXAMPLE: ${EXAMPLE:-01_hello_server}
    container_name: socketlab-client
    networks:
      - socketlab-net
    depends_on:
      - server
    stdin_open: true
    tty: true
    # Pass a different host via: docker compose run client ./client <host>
    command: ["./client", "server"]

networks:
  socketlab-net:
    driver: bridge
